import { Routes } from '@angular/router';
import { RelationsExplorerComponent } from './relations-explorer/relations-explorer.component';

export const routes: Routes = [
  { path: '', component: RelationsExplorerComponent },
];